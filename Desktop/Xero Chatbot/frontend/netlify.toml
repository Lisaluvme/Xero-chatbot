# Netlify Configuration for Xero Chatbot Frontend

[build]
  # No build command needed for static files
  publish = "."

[[redirects]]
  # SPA fallback - redirect all routes to index.html
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
